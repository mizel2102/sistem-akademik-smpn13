Reference: https://www.sman1yogya.sch.id/id

Purpose
-------
This document records how the external site above is used as a visual reference for this project.

Guidelines
----------
- The external site is only a reference for layout, spacing, color, and component structure.
- Do NOT copy textual content (articles, names, addresses), images, or other assets from the reference site into this repository unless you have explicit permission.
- If an asset from the reference site is required, add it to `public/images/` and document its source and license here.
- Replace any temporary or placeholder content with your project's own content (DB-driven) before publishing.

Actions Taken
-------------
- Static content and downloaded reference images were removed and replaced with neutral placeholders.
- `scripts/download_reference_images.ps1` was deleted to prevent automatic copying of external assets.

If you want me to map specific components (header, hero, cards) from the reference site into reusable Blade partials and CSS tokens (without copying content), tell me which components to map and I'll scaffold them.
